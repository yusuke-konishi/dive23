<div class="content">

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>
  <h2>すぐにご利用いただけますので、あなたのスタック・オーバーフロー アカウントを作成ください。</h2>
  <br>


  <div class="field">
    <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "メールアドレス（必須ですが、公開はされません）" %>
  </div>
  <br>
  <br>

  <div class="field">
    <% if @minimum_password_length %>
    (最低 <%= @minimum_password_length %> 文字)
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "パスワード" %>
  </div>
  <br>
  <br>


  <div class="field">
    <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control", placeholder: "パスワード（確認）" %>
  </div>
  <br>
  <br>

  <div class="actions">
    <%= f.submit "登録", class: "btn btn-primary btn-block" %>
  </div>

<br>
<br>

<%= render "devise/shared/links" %>

<% end %>

</div>
<br><br>
