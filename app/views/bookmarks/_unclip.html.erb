<%= form_for(current_user.bookmarks.find_by(question_id: question.id), method: :delete, data: { remote: true }) do |f| %>
  <%= f.hidden_field :question_id %>
  <%= f.submit "★", class: "btn btn-warning btn-block" %>
<% end %>
